// Here in this file we can define the User class, used globally in the application
// So we need to include the "export" keyword to make the class available
export class User {
	id: number;
	fullName: string;
	email: string;
	mobile: string;
	city: string;
	country: string;
	login: string;
	password: string;
}